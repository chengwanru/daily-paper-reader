---
title: "ReWorld: Learning Better Representations for World Action Models"
title_zh: ReWorld：为世界动作模型学习更好的表征
authors: "Tianze Xia, Lijun Zhou, Kaixin Xiong, Jingfeng Yao, Yu Zhu, Zhenxin Zhu, Bing Wang, Guang Chen, Hangjun Ye, Wenyu Liu, Haiyang Sun, Xinggang Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27504v1"
tags: ["query:video-wam"]
score: 9.0
evidence: 学习更好的世界动作模型表示
tldr: "现有世界动作模型(WAM)主要关注架构设计，忽略了中间表示的学习。ReWorld首次提出针对自动驾驶WAM的表示学习框架，通过未来预测监督、跨模态对齐和安全边界判别性监督三个维度直接优化中间表示。在nuScenes和NAVSIM上，生成质量FVD降低23.9%，闭环PDMS提升至90.4，训练收敛加速约2倍，无需后训练或后处理。该工作揭示了中间表示优化对WAM性能的关键作用，为自动驾驶世界模型提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27504v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1338, \"height\": 836, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27504v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1393, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27504v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1402, \"height\": 386, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 741, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1243, \"height\": 595, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1461, \"height\": 666, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 838, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 547, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 373, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 305, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27504v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 890, \"height\": 444, \"label\": \"Table\"}]"
motivation: 现有WAM训练仅监督输出端，中间表示仅作为副产品间接优化，限制了世界知识的有效学习。
method: 提出ReWorld，对Video DiT施加未来预测监督，对Action DiT进行跨模态对齐和安全边界硬负样本监督，直接优化中间表示。
result: "在nuScenes和NAVSIM上，FVD从81.3降至61.9（改善23.9%），闭环PDMS从89.1提升至90.4，从头训练收敛加速约2倍。"
conclusion: 直接优化中间表示可显著提升WAM的生成与规划性能，为自动驾驶世界模型表示学习提供了有效新方法。
---

## 摘要
世界动作模型（WAMs）在动作条件下建模未来环境演化，为自动驾驶提供了一种可扩展的范式。然而，现有方法主要关注模型架构设计，而WAM如何高效学习用于规划的更好世界表征仍未被充分探索。为解决这一问题，我们提出了ReWorld，这是首个专门为自动驾驶世界动作模型设计的表征学习框架。在WAM中，标准训练仅监督生成模块和规划模块的输出端，而承载世界知识的中间表征仅作为拟合这些输出的副产品被间接塑造。ReWorld的核心思想是将中间表征视为优化的直接目标，沿着三个互补维度对其进行塑造。在负责生成的Video DiT上，我们对其中间表征施加未来预测监督。在负责规划的Action DiT上，我们首先将其中间表征与视频世界表征进行跨模态对齐，然后通过硬负样本监督进一步使其在安全关键边界附近具有判别性。此外，我们系统分析了现有表征学习方法在视频生成世界模型中的有效性，并讨论了为何它们在该任务上性能有限。在nuScenes和NAVSIM上的实验表明，ReWorld将微调视频生成的FVD提升了23.9%（从81.3降至61.9），将闭环PDMS从89.1提升至90.4，而无需任何后训练（如强化学习或后处理），并且将从零开始的收敛速度提升了约2倍。

## Abstract
World Action Models (WAMs) model future environment evolution under action conditioning, offering a scalable paradigm for autonomous driving. However, existing approaches focus largely on model architecture design, and how a WAM can efficiently learn better world representations for planning remains underexplored. To address this gap, we propose ReWorld, the first representation learning framework specifically designed for autonomous-driving world action models. In WAMs, standard training supervises only the output ends of the generation and planning modules, leaving the intermediate representations that carry world knowledge to be shaped only indirectly, as byproducts of fitting these outputs. The core idea of ReWorld is to treat intermediate representations as direct targets of optimization, shaping them along three complementary dimensions. On the Video DiT responsible for generation, we impose future-predictive supervision on its intermediate representations. On the Action DiT responsible for planning, we first align its intermediate representations cross-modally with the video world representation, then further shape them to be discriminative around safety-critical boundaries via hard-negative supervision. In addition, we systematically analyze the effectiveness of existing representation learning methods in video generation world models, and discuss why their performance is limited on this task. Experiments on nuScenes and NAVSIM show that ReWorld improves fine-tuned video generation by 23.9% in FVD (81.3 to 61.9), raises closed-loop PDMS from 89.1 to 90.4 without any post-training such as RL or post-processing, and accelerates from-scratch convergence by approximately 2x.

---

## 论文详细总结（自动生成）

# ReWorld: Learning Better Representations for World Action Models 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- 现有世界动作模型（WAM）主要关注架构设计，而标准训练仅监督生成模块（Video DiT）和规划模块（Action DiT）的输出端（生成损失和轨迹规划损失）。
- 中间承载世界知识的表示从未被直接优化，仅作为拟合最终输出的副产品被间接塑造。这导致生成质量与规划能力脱钩：一个WAM可以生成逼真未来帧，却不产生更好的规划。
- 虽然图像扩散模型中的表示学习（如REPA、SRA）已展现潜力，但它们直接迁移到自动驾驶视频生成存在困难：视频需联合语义、时序一致性、状态可达性；外部编码器带来计算开销且与视频时空表征兼容性差。
- 核心目标：在不依赖外部监督或增加计算成本的前提下，提升WAM内部世界表示的学习质量，使生成与规划受益。

## 2. 方法论：核心思想、关键技术细节、算法流程

### 2.1 核心思想
将中间表示作为**直接优化目标**，沿三个互补维度进行塑造：未来预测性世界表示、世界对齐的动作表示、安全感知的动作表示。

### 2.2 技术细节

#### 三阶段训练流程

**Stage 1：未来预测性世界表示（训练Video DiT）**
- 在选定的中间Video DiT层（如第8层）上添加轻量级辅助预测头，预测与主头相同的速度目标（flow-matching velocity）。
- 损失：`LMid = Σ_l E[||ˆv_t^{(l)} - (ε_z - z_0)||²]`
- 推理时利用浅层与深层预测的差异作为自引导信号：`v_w = v_i + γ(v_f - v_i)`，用`v_w`替代`v_f`推进去噪轨迹。
- 目标：`L_Video = L_Gen + λ_Mid L_Mid`

**Stage 2：世界对齐的动作表示（训练Action DiT，Video DiT冻结）**
- 在Action DiT的cross-attention层中，强制动作令牌的隐状态与它关注的视频读出特征在余弦相似度上对齐。
- 损失：`L_align = Σ_k Σ_i [1 - cos(a_i^{(k)}, sg(r_i^{(k)}))]`
- 对视频读出使用stop-gradient防止扰动视频分支。
- 目标：`L_act^(2) = L_FM + λ_align L_align`

**Stage 3：安全感知的动作表示（联合微调两个DiT）**
- 为每个训练场景离线构建候选轨迹池（N=64），利用NAVSIM PDM模拟器评分，选出几何上接近专家但评分低（<0.6）的硬负样本。
- 定义delta表示编码相对运动，最大化预测轨迹与硬负样本在delta空间的距离。
- 损失：`L_RDE = - (1/|V|) Σ_b (1/L) Σ_ℓ (1/4) Σ_d |Δ(ˆτ_b)^{(d)}_ℓ - Δ(τ_neg_b)^{(d)}_ℓ|`
- 目标：`L_act^(3) = L_FM + λ_RDE L_RDE`
- 该损失梯度可回传到视频分支，使生成先验也内化闭环行为区分。

### 2.3 算法流程总结
1. 先用`L_Gen + λ_Mid L_Mid`训练Video DiT。
2. 冻结Video DiT，用`L_FM + λ_align L_align`训练Action DiT。
3. 解冻两个DiT，用`L_FM + λ_RDE L_RDE`联合微调。

## 3. 实验设计

### 3.1 数据集与场景
- **训练集**：nuPlan（约1200小时真实驾驶数据，4个大都市区域）+ nuScenes（1000个城市驾驶序列，850开发/150测试）。采样8Hz视频帧，轨迹监督使用2Hz帧（来自NAVSIM）。
- **评估**：
  - 视频生成：nuScenes验证集（FVD指标）。
  - 规划：NAVSIM（基于nuPlan构建的非反应式闭环基准），包含Navtrain（约10.3万场景）和Navtest（约1.2万场景）。
- **Benchmark指标**：
  - 视频：Fréchet Video Distance (FVD)，不报告FID（不反映时序一致性）。
  - 规划：PDMS（由NC、DAC、TTC、Comf.、EP加权乘积计算）。

### 3.2 对比方法
- **视频生成**（表1）：DriveGAN、DriveDreamer、DrivingGPT、Vista、Epona、DriveLaW
- **表示学习方法对比**（表2）：Vanilla Flow、SRA、SRA2、Self-Flow、ReWorld（无外部编码器）；REPA（带DINOv2/VideoMAEv2/DepthAnything3/V-JEPA2）、ReDi（有外部编码器）
- **规划**（表3）：传统端到端方法（VADv2、UniAD、TransFuser、PARA-Drive、ReCogDrive-IL、DiffusionDrive）和世界模型方法（DrivingGPT、LAW、Epona、ReSim、WoTE、DriveVLA-W0、PWM、WorldDrive、DriveLaW）

## 4. 资源与算力

- 论文**未明确说明GPU型号和数量**，但给出了训练步数、批量大小和优化器配置：
  - Stage 1（Video DiT）：全局batch size 64，20k步，AdamW（lr=1e-5, weight decay=5e-2）。
  - Stage 2（Action DiT冻结视频）：全局batch size 128，6k步。
  - Stage 3（联合微调）：全局batch size 160，10k步。
- 计算效率对比（表8）：ReWorld在Video DiT训练中仅增加0.3%开销（1.003×），远低于REPA（~1.7×）、ReDi（~1.6×）、Self-Flow（~1.4×）等。
- 未提供具体硬件型号和时间是论文的一个信息缺口。

## 5. 实验数量与充分性

- **总计约8个实验表格**，覆盖：
  - 主结果（表1、表3）：视频生成和规划。
  - 表示学习方法对比（表2）：9种变体统一协议从零训练120k步。
  - 消融实验（表4-8）：
    - 规划组件逐步消融（对齐+硬负样本）。
    - 监督层选择（层2/8/12/16/20）。
    - 自引导强度γ（1.0/1.2/1.4/1.6/1.8）。
    - 硬负样本损失权重λ_RDE（0.02/0.03/0.04/0.05/0.10）。
    - 计算效率对比（8种方法）。
- **充分性分析**：实验设计较为全面，消融覆盖了关键超参数和组件贡献；对比方法覆盖主流；但缺少在更多驾驶数据集（如Waymo）上的验证，且仅在nuScenes和NAVSIM上评估，泛化性待考。整体客观公平，使用统一协议控制变量。

## 6. 主要结论与发现

- ReWorld在保持几乎相同计算开销下，显著提升视频生成质量和规划性能：
  - 微调FVD从81.3降至61.9（改善23.9%）。
  - 闭环PDMS从89.1提升至90.4（+1.3），无需RL或后处理。
  - 从头训练收敛速度约加速2倍。
- 中间表示直接优化是有效的：未来预测监督使Video DiT获得更强时序一致性；跨模态对齐使世界知识更好传递给规划器；硬负样本监督赋予安全判别性。
- 对比实验表明：外部编码器方法（如REPA）在长程视频生成中可能引入有害梯度或干扰时空表示；自监督内部对齐（ReWorld）更稳定高效。

## 7. 优点

1. **创新性**：首次将中间表示直接优化引入WAM，针对生成和规划分别设计互补的表示增强机制。
2. **高效性**：所有机制在模型自身潜空间内运作，无需外部编码器或教师模型，计算开销极小（仅增加0.3%）。
3. **系统性**：三阶段渐进式训练，逐步注入未来预测、跨模态对齐、安全感知，逻辑清晰且可解释。
4. **实验充分**：包含大量消融和对比，验证各组件贡献；统一协议比较表示学习方法的迁移效果，揭示其局限性。
5. **实用结果**：无需后训练即提升闭环规划分数，对自动驾驶部署有实际价值。

## 8. 不足与局限

1. **依赖特定架构**：基于DriveLaW链式结构（Video DiT + Action DiT），表示学习方法可能不直接推广到其他WAM架构（如共享骨干、耦合设计）。
2. **硬负样本构建依赖离线模拟器评分**：计算成本较高，且评分准确性影响监督质量；未分析对模拟器误差的鲁棒性。
3. **评估范围有限**：仅在nuScenes和NAVSIM上实验，缺少在多样化场景（如恶劣天气、夜间、Waymo等）和多视图设置下的验证。
4. **消融缺少部分维度**：未消融λ_align超参数对最终性能的敏感度；未比较不同硬负样本选择策略（如随机负样本 vs 硬负样本）。
5. **计算资源未公开**：GPU型号、数量、总训练时间缺失，影响可重复性。
6. **安全指标单一**：仅使用PDMS，未报告碰撞率等更直接的安全度量；硬负样本基于几何接近性，可能忽略其他危险模式（如交通规则违反）。

（完）
