---
title: "A Path-Space Formulation of Prediction in World Models: From a Single Action to Prediction, Planning, and Irreversibility"
title_zh: 世界模型中预测的路径空间表述：从单一动作到预测、规划与不可逆性
authors: Gunn Kim
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28751v1"
tags: ["query:video-wam"]
score: 9.0
evidence: 世界模型中预测的路径空间公式
tldr: 现有世界模型通常基于单步条件分布序列，本文提出路径空间公式，将世界模型理解为未来轨迹的概率测度。在局部马尔可夫近似下，该测度具有Onsager-Machlup形式，使预测、规划和不确定性统一为作用量函数的操作。通过分解潜动态为可逆与不可逆部分，引入模型rollout的熵产生度量。实验表明注意力不对称性与数据不可逆性相关，对称化会抑制熵产生并选择性损害不可逆长程预测，揭示不可逆性可作为预测世界模型的计算资源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28751v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 615, \"height\": 223, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1831, \"height\": 1174, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 1187, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28751v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1835, \"height\": 1548, \"label\": \"Figure\"}]"
motivation: 现有世界模型以单步条件分布预测，缺乏对轨迹整体概率的显式建模，难以统一处理长期预测、规划和不可逆性。
method: 提出路径空间公式，将世界模型隐式定义为未来轨迹测度，在局部马尔可夫假设下导出Onsager-Machlup作用量函数，并分解动态为可逆和不可逆分量，定义熵产生度量。
result: 在注意力模型上，发现注意力不对称性与数据不可逆性正相关；对称化注意力可抑制熵产生，并选择性降低不可逆动态的长程预测性能。
conclusion: 不可逆性可以作为预测世界模型的一种计算资源，路径空间视角为统一预测、规划和不确定性提供了新框架。
---

## 摘要
我们提出了一种AI世界模型中预测的路径空间表述。不同于单步条件分布序列，我们认为世界模型隐含地定义了未来轨迹上的概率测度。在潜在动力学允许有效马尔可夫描述的局部范围内，该路径测度采用Onsager-Machlup形式。在此框架内，预测（最可能轨迹）、规划（约束优化）和不确定性（涨落）表现为对单一动作泛函的操作。我们将潜在动力学分解为可逆和不可逆分量，并引入基于模型 rollout 的熵产生操作度量。在受控的小规模注意力模型中，我们发现注意力不对称性在训练过程中与数据的不可逆性成比例地获得。对称化学习到的注意力会抑制熵产生，并选择性地降低不可逆动力学的长时域预测性能，同时保持松弛预测。这些结果表明，不可逆性可能作为预测性世界模型的一种计算资源。更一般地，基本的预测对象是未来路径上的分布，而非状态上的分布。

## Abstract
We propose a path-space formulation of prediction in AI world models. Rather than sequences of one-step conditional distributions, we argue that a world model implicitly defines a probability measure over future trajectories. In the local regime where latent dynamics admit an effective Markovian description, this path measure takes the Onsager-Machlup form. Within this framework, prediction (most probable trajectory), planning (constrained optimization), and uncertainty (fluctuations) emerge as operations on a single action functional. We decompose the latent dynamics into reversible and irreversible components and introduce operational measures of entropy production from model rollouts. In controlled small-scale attention-based models, we find that attention asymmetry is acquired during training in proportion to the irreversibility of the data. Symmetrizing the learned attention suppresses entropy production and selectively degrades long-horizon prediction of irreversible dynamics while preserving relaxational prediction. These results suggest that irreversibility may serve as a computational resource for predictive world models. More generally, the fundamental predictive object is a distribution over future paths rather than states.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有世界模型通常基于单步条件分布（如 `p(x_{t+1} | x_t)`）进行预测，但作者认为这种“下一状态预测”的范式忽略了轨迹整体的概率结构，无法自然地统一处理预测、规划与不确定性。
- **研究动机**：借鉴统计物理中的路径积分和随机热力学思想，将世界模型视为未来轨迹的**概率测度**，而非单个状态的条件分布。通过引入Onsager-Machlup作用量，使得预测、规划、不确定性均成为对同一泛函的操作。
- **整体含义**：世界模型的本质是路径分布，其不可逆性（熵产生）不仅是学习副产品，更可以是维持长时域预测能力的**计算资源**。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  - 将未来轨迹 `Γ = {z(t)}` 视作整体，世界模型定义其上概率测度 `P[Γ]`。
  - 在潜在的局部马尔可夫近似下，该测度具有**Onsager-Machlup作用量**形式：`P[Γ] ∝ exp(-A[Γ])`。
  - 作用量 `A[Γ]` 包含动能项 `||ẑ - f||²` 和雅可比修正 `½ ∇·f`，其时间反演不对称部分正比于熵产生。
- **关键技术细节**：
  - **预测**：最可能轨迹由 `δA = 0` 给出，满足欧拉-拉格朗日方程，与确定性 rollout 不同（除非漂移散度均匀）。
  - **规划**：固定终点的最小作用量问题，目标值为路径自由能。
  - **不确定性**：作用量二阶变分给出涨落算子的格林函数 → 预测协方差。
  - **注意力与作用量对应**：注意力层的 `M = W_Q^T W_K` 作为度量；softmax 归一化给出雅可比项；查询-键不对称性 `M_A` 直接影响漂移雅可比的反对称部分 `J_A`，进而产生不可逆漂移 `v`。
  - **熵产生测量**：从 rollout 数据估计漂移 `̂f`、扩散 `̂D`、稳态密度 `ρ_ss`，通过 `v = ̂f - ̂D ∇ ln ρ_ss` 计算不可逆漂移，熵产生率 `˙Σ = ⟨|v|²⟩_ss / ̂D`。
- **算法流程（文字说明）**：
  1. 从训练数据中学习注意力参数 `W_Q, W_K`。
  2. 在潜空间进行 rollout，收集轨迹样本。
  3. 利用 Kramers-Moyal 系数估计漂移 `̂f` 和扩散 `̂D`（网格法）。
  4. 由 rollout 经验分布估计稳态密度 `ρ_ss`。
  5. 计算概率流 `J = ̂f ρ_ss - ̂D ∇ ρ_ss`，得到不可逆漂移 `v = J / ρ_ss`。
  6. 计算熵产生率 `˙Σ = ∫ |J|² / (̂D ρ_ss) dz`。
  7. 计算注意力不对称指标 `Q = ∥M_A∥_F / ∥M∥_F`。
  8. 验证 `˙Σ` 与 `Q` 的相关性，并通过对称化 `M → Sym(M)` 进行因果干预。

## 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **数据和场景**：
  - 使用**受控的二维合成数据**：线性漂移 `dz = (-γz + ωRz)dt + √(2D)dW`（R为旋转生成器），可调节 `ω` 控制不可逆程度（0表示纯梯度松弛，非零表示循环运动）。
  - 两种模型架构：线性注意力（精确可解）和 softmax 注意力（固定记忆槽）。
- **Benchmark**：
  - 对线性模型，精确熵产生由 Lyapunov 方程给出，作为对照。
  - 对“rollout error”：对比训练好模型、对称化后模型与真实条件均值 `μ(z, h)` 的多步预测误差。
- **对比方法**：
  - 主要在**对称化干预前后**对比：同一模型在 `Q→0`（强制 `W_Q = W_K`）前后的熵产生、概率流、长程预测误差。
  - 没有与其他世界模型（如 Dreamer、RSSM）直接对比，因为实验是机制验证性的小规模受控实验。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力。若未明确说明，也请指出这一点。
- **未明确说明**。论文使用小规模 2D 潜空间 + 少量记忆槽，训练仅需 CPU 或单张 GPU 即可快速完成，未涉及大规模 GPU 集群或长时间训练。作者声明这是“controlled minimal model”，算力需求极小。

## 5. 实验数量与充分性：大概做了多少组实验，是否充分、客观、公平
- **主要实验**：
  - **线性模型**：在 `θ`（不对称度）从 0 到 1 以上扫描，测量熵产生（图1a）。
  - **Softmax 模型**：类似扫描（图1b）。
  - **对称化干预试验**：分别对线性和 softmax 模型进行干预，比较熵产生前后变化（图1c）以及概率流结构（图1d,e）。
  - **训练动力学实验**（图2）：从对称初始化，在循环数据上训练，记录训练步数 vs `∥M_A∥` 与 `˙Σ`（图2a）；在不同 `ω`（数据不可逆性）下训练，对比 `˙Σ` 与理论值（图2b）、不对称性与非正态性（图2c）；对称化干预后对循环与松弛数据的一步损失变化（图2e）。
  - **长程预测对比**（图3）：对循环和松弛数据，对比训练模型与对称化模型的多步 rollout 误差（图3a,b）；峰值误差与熵产生关系（图3c）；示例轨迹（图3d）。
- **充分性评价**：
  - **充分**：覆盖了核心机制验证的完整链条——相关性、因果干预、长程预测退化、选择性影响。
  - **客观**：对线性模型有精确解验证，测量结果与理论吻合。干预是确定性的参数替换。
  - **公平**：在同一模型上唯一变化的是 “是否对称化”，消解了其他混淆因素。但**局限性**在于仅限 2D 潜空间和小规模，未扩展到高维真实世界模型。

## 6. 论文的主要结论与发现
- **主要结论**：
  1. **查询-键不对称性** `M_A` 是潜动态不可逆性的**架构可控来源**，它通过驱动反对称雅可比 `J_A` 产生循环漂移 `v` 和熵产生。
  2. **不对称性是习得的**：训练过程中，模型在循环数据上自然地学到 `M_A`，且其大小与数据不可逆性成正比。
  3. **不可逆性是一种计算资源**：对称化注意力显著降低熵产生，同时**选择性破坏不可逆动态的长程预测**（循环运动），但不影响松弛预测。
  4. **预测、规划、不确定性可统一为对路径测度作用量的操作**：最可能轨迹、固定端点规划、涨落协方差均由同一 Onsager-Machlup 作用量导出。
- **关键发现**：世界模型若需预测具有时间结构（如旋律、运动序列）的未来，必须**打破细致平衡**，其架构应保留不对称性。熵产生是这一资源的直接度量。

## 7. 优点：方法或实验设计上有哪些亮点
- **理论深度**：将路径积分、随机热力学、注意力机制紧密结合，提供了全新的理论视角。
- **因果性验证**：不仅展示相关性（`Q` 与 `˙Σ` 正相关），还直接执行**参数干预**（对称化），并观察到预测表现的**选择性退化**，这是因果证据而非单纯关联。
- **精确可验证**：对线性模型有解析解对照，测量管线（Kramers-Moyal + 网格密度估计）被验证可恢复真实熵产生。
- **简洁性**：使用最小模型（2D 潜空间）揭示本质机制，避免过大模型的复杂干扰，使结论清晰。
- **操作定义明确**：每个量（漂移、熵产生、注意力不对称性、非正态性）都有从 rollout 数据中提取的具体算法，可复现。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **实验覆盖有限**：
  - 仅在**二维潜空间**验证，高维（如常见世界模型 256 维以上）中注意力不对称性是否为熵产生的主要来源尚不明确。
  - 假设“局部马尔可夫性”成立，但现实世界模型可能存在长程记忆（非马尔可夫），其记忆参数 `ϵ` 的影响只有理论定义，未实验测量。
  - 仅测试了**值绑定**（value-tied）的注意力层，实际模型中值路径可独立学习，不可逆性的架构通道更多样。
- **偏差风险**：
  - 网格熵产生估计在 `Q≈0` 时存在正偏（约 0.2 的噪声底），可能高估弱不可逆系统的熵产生。
  - 对称化干预完全通过权重修改实现，但 softmax 导致的**状态依赖非可积性**可能残留，论文虽提及但未彻底分离。
- **应用限制**：
  - 未在大型预训练世界模型（如 Dreamer、RSSM、Transformer-based 序列模型）上验证；作者仅建议了未来协议，未执行。
  - 规划与不确定性的实验未实际开展（论文只做了理论推导和预测验证部分），留下待测的实证缺口。
  - 需要设计可扩展的高维熵产生估计器（如变分下界、得分匹配），当前网格法无法推广到高维。

（完）
