---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：一种统一的世界与动作建模扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:video-wam"]
score: 10.0
evidence: 使用单一扩散变压器统一动作生成与视觉世界建模，明确用于世界和动作建模。
tldr: 现有机器人策略常依赖大型预训练视觉语言模型作为动作骨干，导致模型庞大且计算成本高。WorldDiT提出一种统一扩散transformer架构，将动作生成与视觉世界建模耦合，无需大型VLM。在四个LIBERO仿真套件中，WorldDiT以低于十亿参数实现了帕累托最优的参数量与成功率平衡，为未来规模研究提供了强基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 依赖大型VLM动作骨干的策略模型庞大，计算开销高，限制了实际部署与扩展。
method: WorldDiT采用单个扩散transformer，联合生成连续动作块并预测未来相机帧的归一化RGB patch目标。
result: 在LIBERO四个套件上，WorldDiT位于总参数与平均成功率的帕累托前沿，以0.7B参数取得竞争性性能。
conclusion: 证明了不依赖大型VLM的紧凑动作-世界联合建模的有效性，为机器人策略的轻量化与扩展提供了新基线。
---

## 摘要
许多近期机器人策略通过使用大型预训练视觉-语言模型作为动作主干来追求更强的控制能力。我们提出WorldDiT，一种统一的扩散Transformer架构，将动作生成与视觉世界建模耦合，无需大型预训练视觉-语言模型动作主干即可实现强性能。训练过程中，单个扩散Transformer生成连续动作块，并从未来相机帧预测归一化RGB块目标。在四个LIBERO仿真套件中，WorldDiT位于报告的总参数数量与平均成功率的帕累托前沿上（针对报告所有四个套件的方法）。这些结果为未来的扩展研究提供了一个强大的十亿参数以下基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

# WorldDiT：一种统一的世界与动作建模扩散架构 —— 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前许多机器人策略（policy）依赖大型预训练视觉-语言模型（VLM）作为动作骨干，模型参数高达数十亿，导致难以区分规模效应和架构贡献。作者质疑：**是否可以在不使用大型VLM动作骨干的前提下，通过统一架构同时完成动作生成与视觉世界建模，并保持强控制性能？**
- **整体含义**：WorldDiT提出一个**十亿参数以下**的紧凑基线模型，证明了“世界-动作联合建模”策略的有效性，为未来扩展到更大规模提供了基础。它位于LIBERO基准中参数-成功率帕累托前沿，表明强控制能力不一定需要巨大模型。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **统一扩散Transformer**：单个DiT骨干同时学习连续动作生成和未来视觉预测（归一化RGB块），但在推理时仅使用动作路径，视觉预测只在训练中提供辅助监督。
- **不依赖大型预训练VLM**：仅使用冻结的视觉编码器（MAE）和文本编码器（CLIP）以及可训练的机器人状态编码器进行条件注入。

### 关键技术细节
1. **多模态输入**：
   - 观测窗口：3个上下文步骤（主相机+腕相机图像、机器人状态）+ 7个动作目标 + 1个未来RGB块目标。
   - 每个未来RGB帧分为16×16块，提取64个均匀间隔的归一化块（每相机），两个相机共128个768维向量。

2. **训练流程（流匹配）**：
   - 对动作目标和RGB块目标施加高斯噪声，定义线性插值路径：\( x_\tau = (1-\tau)\epsilon + \tau y \)。
   - 损失函数：\( \mathcal{L}_{\text{flow}} = \mathbb{E}[\|v_\theta(x_\tau,\tau,c) - (y-\epsilon)\|_2^2] \)，总损失为动作损失和RGB块损失的加权和（权重0.1和0.001）。

3. **推理流程（动作生成）**：
   - 从高斯噪声开始，用20步Euler积分预测动作速度，得到7步动作块，执行前3步后重规划（滑动窗口+时间集成）。

## 3. 实验设计

### 数据集与基准
- **LIBERO仿真基准**：使用四个下游套件（`spatial`, `object`, `goal`, `long`）进行微调和评估，并在大型多任务`libero_90`上预训练。
- **任务类型**：包含空间操作、物体操作、目标达成、长序列操作等，共数十个任务。

### 对比方法
- 共24种方法，分为两类：
  - **使用大型预训练VLM动作骨干**（如ACoT VLA, MMaDA VLA, π0.5, OpenVLA等15种）
  - **不使用大型预训练VLM动作骨干**（如WorldDiT, DreamVLA, FlowVLA, Octo, Diffusion Policy等8种）
- 关键对比：WorldDiT以399M总参数（含冻结模块）取得94.9%平均成功率，位于所有报告方法的帕累托前沿。

## 4. 资源与算力

- **训练硬件**：1个节点，8块**RTX Pro 6000 GPU**（48GB显存）。
- **训练配置**：
  - 预训练：`libero_90`上30个epoch，每GPU batch size 40，梯度累积2，学习率1e-4（余弦调度，1个warmup epoch）。
  - 微调：每个下游套件独立微调，有效batch size 512（每GPU 32，梯度累积2），bf16混合精度。
  - 推论：20步Euler采样，使用EGL无头渲染。
- **未明确说明**：总训练时间、预训练和微调分别耗时多长未提及。

## 5. 实验数量与充分性

- **实验数量**：对每个LIBERO套件评估500个模拟回合（其中300回合用于阶段性检查点选择，非完全独立测试）。报告了四个套件的成功率及均值。
- **公平性与充分性**：
  - ✅ **优势**：与24种方法在同一基准上对比，参数-成功率关系清晰；使用标准LIBERO划分和评估协议。
  - ⚠️ **不足**：
    - 作者承认报告的成绩包含用于检查点选择的300回合，**非完全独立测试**，94.9%不应被当作无偏估计。
    - **缺少消融实验**：未验证未来RGB预测目标、损失权重、动作块长度、流步数等关键组件的影响。
    - **缺少多场景泛化**：仅在LIBERO仿真环境评估，未在真实机器人上验证。
    - 对比方法中部分为近似重构的参数计数，且Seer未报告全部四个套件，被排除在帕累托计算之外。

## 6. 论文的主要结论与发现

1. **世界-动作联合训练可行**：统一DiT架构同时学习动作和视觉预测，推理时仅用动作路径，仍达94.9%平均成功率。
2. **无需大型VLM骨干**：399M参数（仅135M可训练）即可达到与数B参数VLA方法竞争的性能，位于帕累托前沿。
3. **LIBERO长序列依然困难**：`long`套件成功率最低（91.8%），与整体趋势一致。
4. **提供强基线**：为未来参数规模扩展研究提供了十亿参数以下的有效基准。

## 7. 优点

- **架构简洁高效**：统一的DiT骨干，避免了复杂多阶段或大型VLM依赖，推理时轻量。
- **流匹配训练**：任务统一为速度预测，动作和视觉损失共享公式，便于扩展和分拆（如Paris系列）。
- **参数效率突出**：在相同参数规模下性能最优，优于Octo、Diffusion Policy等无VLM方法。
- **客观性能对比**：系统性地在24种方法中进行参数-性能帕累托分析，清晰展示其定位。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅在一个仿真基准（LIBERO）上评估，未涉及真实机器人、其他仿真环境或不同物体/场景泛化。
  - 每个套件500回合（含非独立测试回合）的样本量偏小，特别是`long`套件任务复杂但回合数相同。
- **缺乏关键消融**：
  - 未验证未来RGB预测目标对性能的具体贡献（如果移除是否会下降？）
  - 未测试不同动作块长度、流步数、损失权重的影响。
- **偏差风险**：
  - 报告分数包含检查点选择回合，不是完全独立测试，可能高估真实性能。
  - 对比方法中部分参数计数为近似重构，可能存在细微差异。
- **应用限制**：
  - 方法假设多视角固定相机、语言指令明确、离散任务（LIBERO典型场景），在开放世界或长时域交互中可能面临挑战。
  - 冻结MAE和CLIP编码器可能限制了视觉表示的泛化能力。
- **未开源**：论文未提供完整模型权重或训练代码（仅提及部分公开），可复现性受限。

（完）
