---
title: "MiniWorld: Democratizing the Training of Video World Models from Scratch"
title_zh: MiniWorld：从零开始民主化训练视频世界模型
authors: "Yian Zhao, Ruochong Zheng, Hongcan Guo, Yu Yan, Jian Zhang, Jie Chen"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01127v1"
tags: ["query:video-wam"]
score: 10.0
evidence: 从零训练视频世界模型学习智能体动作下的环境动态，是视频世界模型的核心工作
tldr: 视频世界模型需预测未来观测并学习环境动态，现有方法依赖预训练模型微调，成本高且存在训练推理不一致。MiniWorld提出从零训练流式视频世界模型的轻量框架，采用块因果Video Diffusion Transformer与Flow Matching，结合分块非递减噪声调度和两阶段训练，推理时使用滚动KV缓存与异步去噪。仅需单台8卡服务器数天即可训练完成，代码和权重开源，为后续研究提供可复现基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01127v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1704, \"height\": 1045, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 1060, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1721, \"height\": 930, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 842, \"height\": 450, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 847, \"height\": 406, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01127v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1172, \"height\": 369, \"label\": \"Table\"}]"
motivation: 现有视频世界模型多依赖预训练生成模型，训练复杂、资源消耗大，且双向预训练与因果推理不匹配，缺乏轻量可复现的从零训练基线。
method: 采用块因果Video Diffusion Transformer和Flow Matching，基于Diffusion Forcing设计分块非递减噪声调度，经两阶段持续训练，推理用滚动KV缓存和异步去噪。
result: 整个模型在单台8-GPU服务器上数天内可训练完成，实现高效流式生成，并开源代码和预训练权重。
conclusion: MiniWorld提供了透明可复现的从零训练视频世界模型基线，有望推动该领域的后续研究。
---

## 摘要
视频世界模型基于历史观测和控制信号预测未来观测，通过自回归状态转换实现长时程生成。与主要捕捉视觉外观和运动的传统视频生成模型不同，视频世界模型学习智能体动作作用下环境演化的底层动力学，为具身智能和交互式模拟提供基础。近期进展在很大程度上依赖通过对预训练视频生成模型进行后训练或蒸馏来适配。尽管有效，但这些方法通常需要复杂的训练流程、大量的计算资源，并存在双向预训练与因果流式推理之间的不匹配问题。近期研究表明，从零开始训练自回归视频世界模型是可行且可扩展的。然而，社区仍然缺乏一个轻量级、透明且完全可复现的基线，能够在适度的计算资源下端到端训练。我们提出MiniWorld，一个用于从零开始训练流式视频世界模型的可复现框架。MiniWorld采用块因果视频扩散Transformer，在预训练视频VAE的潜空间中使用流匹配进行训练。基于Diffusion Forcing，它采用块状非递减噪声调度和两阶段持续训练，以改善时间建模和稳定性。在推理时，MiniWorld将滚动KV缓存与流水线异步去噪相结合，在有限计算下实现高效流式生成。整个模型可在单个8-GPU服务器上在几天内完成训练。通过发布训练和推理代码库及预训练检查点，我们希望MiniWorld能促进视频世界建模的未来研究。

## Abstract
Video world models predict future observations conditioned on historical observations and control signals, enabling long-horizon generation through autoregressive state transitions. Unlike conventional video generation models that primarily capture visual appearance and motion, video world models learn the underlying dynamics governing environment evolution under agent actions, providing a foundation for embodied AI and interactive simulation. Recent progress has largely relied on adapting pretrained video generation models through post-training or distillation. Although effective, these approaches often require complex training pipelines, substantial computational resources, and suffer from the mismatch between bidirectional pretraining and causal streaming inference. Recent studies have shown that training autoregressive video world models from scratch is feasible and scalable. However, the community still lacks a lightweight, transparent, and fully reproducible baseline trainable end-to-end with modest computational resources. We present MiniWorld, a reproducible framework for training streaming video world models from scratch. MiniWorld employs a block-causal Video Diffusion Transformer trained with Flow Matching in the latent space of a pretrained Video VAE. Building on Diffusion Forcing, it adopts a chunk-wise non-decreasing noise schedule and two-stage continued training to improve temporal modeling and stability. During inference, MiniWorld combines a rolling KV cache with pipelined asynchronous denoising for efficient streaming generation under bounded computation. The entire model can be trained within several days on a single 8-GPU server. By releasing the training and inference codebase and pretrained checkpoints, we hope MiniWorld will facilitate future research on video world modeling.

---

## 论文详细总结（自动生成）

# MiniWorld：从零开始民主化训练视频世界模型

## 1. 核心问题与整体含义

- **研究背景**：视频世界模型的目标是根据历史观测和控制信号预测未来观测，通过学习环境在智能体动作下的演化动力学，为具身智能、交互式模拟和物理智能提供基础。
- **现有范式的问题**：当前主流方法（如Genie、Cosmos、MAGI-1、SkyReels-V2等）多依赖对预训练双向视频扩散模型（如Wan）进行后训练或蒸馏。这类方案需要复杂多阶段的优化流程和大量计算资源，且存在**双向预训练与因果流式推理之间的结构性不匹配**，导致训练与部署不一致、暴露偏差和长期漂移等问题。
- **缺失的基线**：虽然已有研究（MAGI-1、SkyReels-V2）证明从零训练自回归视频世界模型可行且可扩展，但社区仍缺乏一个**轻量级、透明、完全可复现**、能在适度算力下端到端训练的基线。
- **核心含义**：本文提出MiniWorld，首次在单台8-GPU服务器上于数天内从零训练出可用的流式视频世界模型，将视频世界模型研究从"算力密集型"转向"可复现的学术研究"，为该领域建立开放基线。

## 2. 方法论

### 2.1 整体框架
MiniWorld采用**块因果Video Diffusion Transformer**，在预训练Wan2.2视频VAE的潜空间中使用**Rectified Flow / Flow Matching**进行训练，核心设计围绕"训练-推理一致性"展开。

### 2.2 问题形式化
将视频世界模型建模为**动作条件下的下一状态预测**：给定历史帧和未来动作，预测未来帧序列。与固定窗口条件视频生成范式相比，直接从训练目标上消除双向生成与自回归推理的错配。

### 2.3 核心组件
- **块因果自注意力**：视频序列被划分为时间块，块内双向注意力、块间严格因果注意力，实现局部充分建模与长期因果依赖兼备。
- **动作条件机制**：采用AdaLN（自适应层归一化），动作编码器输出嵌入和调制向量，分别用于扩散时间步语义融合和特征调制；引入**AdaLN-LoRA**共享调制网络加逐层低秩残差，兼顾参数效率和表达力。
- **分块非递减噪声调度**：基于Diffusion Forcing，在训练时为不同块分配独立的扩散时间步，并施加约束 τ₁ ≤ τ₂ ≤ ... ≤ τ_M（非递减），保证早期块更干净、后期块更嘈杂，符合推理时的真实状态。
- **CoPP调度器**：将AR-Diffusion的FoPP扩展至块级——先抽样"锚块"时间步，再在非递减约束下向外传播，保持时间步分布平衡并覆盖多样调度轨迹。
- **两阶段持续训练**：预训练阶段（短序列21→46帧）高效学习局部动态；长视频持续训练阶段（125→253帧）将模型暴露于更长上下文中，配合SD3式时间步偏移（timestep shifting）优化长程建模。
- **流式推理**：采用**滚动KV缓存 + 流水线异步去噪**。已完成块提交到固定容量FIFO缓存，结合永久保留的sink锚帧；通过**RoPE re-shifting**避免位置外推；多块并行去噪形成流水线，在计算资源与生成质量间灵活权衡。

## 3. 实验设计

### 3.1 数据集
- **DROID**：大规模机器人操作数据集，评估低层动作条件下的具身世界建模，动作为7维（6-DoF末端位移+夹爪位置）。
- **RealEstate10K (RE10K)**：相机轨迹数据集，评估相机位姿控制下的场景预测，条件为射线原点和方向。

### 3.2 基线
- **双向短视频基线**：固定29帧滑动窗口预测，同一控制序列下与MiniWorld比较。
- 所有评估以单帧观测为上下文，输出253帧流式生成。

### 3.3 评估指标
PSNR、SSIM、LPIPS，以及WorldArena风格指标（外观、动力学、几何、VLM功能质量、保真度），还涵盖轨迹准确率、深度准确率、光度平滑性、主体一致性等细粒度指标。

### 3.4 实验数量与类型
- **主实验**：DROID和RE10K两个基准，各50个保留测试视频。
- **消融实验**（DROID上5组）：CFG开关、生成长度（253 vs 381帧）、在线去噪窗口（32块 vs 8块+KV缓存）、KV缓存容量（24/12/6块）、sink帧数量（0/1/2）。
- **扩展性分析**：RE10K上0.5B/1B/3B模型对比。
- **吞吐量分析**：DROID上1B模型的流式吞吐、首块延迟、DiT/VAE计算占比。
- **模型配置**：发布了5种规模（B/L/0.5B/1B/3B）的配置表。

## 4. 资源与算力

- **训练环境**：单台配备8块GPU的服务器，使用bf16混合精度训练。
- **GPU型号**：文中未明确说明GPU型号和显存容量。
- **训练时长**：作者声称整个模型可在**数天内**完成训练——具体说"several days on a single 8-GPU server"。
- **优化器**：Muon优化器（短时训练阶段学习率1×10⁻⁴、批量64；长视频持续训练阶段学习率2×10⁻⁵、批量8）。
- **开放资源**：完整代码、预训练权重、数据预处理、训练/推理/评估全流程均已开源。

## 5. 实验数量与充分性分析

### 积极方面
- **双基准验证**：DROID（动作条件）和RE10K（相机条件）覆盖两种主流控制模态，验证了方法的跨域泛化性。
- **消融覆盖较广**：指导缩放、生成长度、在线窗口、缓存容量、sink帧五个关键维度均有消融，且结合质量与效率两个角度评估。
- **开源性增强可验证性**：完全开源为第三方复现和扩展提供了条件。

### 不足与风险
- **基线对比较弱**：仅对比双向短窗基线，未与MAGI-1、SkyReels-V2等同类从零训练方法直接对比，也缺少与后训练型世界模型的对比。
- **评估视频数有限**：每基准仅50个保留视频，统计显著性有限。
- **部分消融结果呈中性**：如sink帧数量消融表明其并非核心增益来源，说明对设计选择的效果归因不够清晰。
- **数据量规模受限**：相比前沿视频基础模型，数据规模和多样性有限，可能限制了结论的泛化性。

## 6. 主要结论与发现

- MiniWorld在**DROID上全面超越双向基线**：轨迹准确率提升249%、深度准确率提升238%、LPIPS提升216%、SSIM提升125%，VLM评分提升63%-78%，尤其在几何和保真度上优势显著。
- 在**RE10K上改进更温和但覆盖面广**：光度平滑性提升89%、深度准确率提升55%、主体一致性提升50%，表明架构可跨模态迁移。
- **KV缓存流式推理是核心效率来源**：8块在线窗口+KV缓存将吞吐从3.31 FPS提升至7.29 FPS（2.2×），首块延迟从74.0秒降至4.86秒（15.2×）。
- **模型扩展性一致提升**：从0.5B到3B，动力学（+22%）、深度准确率（+18%）、图像质量（+14%）、流分数（+12%）均持续改善，说明增量参数主要强化了运动建模和3D一致性。
- **从零训练流式世界模型在适度算力下是可行的**，稳定长程、动作条件的世界建模不需要极高计算门槛。

## 7. 优点

- **轻量可复现的开源基线**：填补了社区缺乏轻量从零训练流式世界模型基线的空白，完整开放代码、权重和管线。
- **训练-推理对齐设计**：从目标函数（块因果自回归）、噪声调度（非递减块噪声）到推理策略（KV缓存+异步去噪），全链路追求一致性，避免双向预训练的结构性缺陷。
- **统一条件接口**：同一架构支持动作条件和相机条件，跨域迁移能力验证了普适性。
- **高效的流式推理设计**：滚动KV缓存、RoPE重定位、流水线异步去噪等技术组合有效解决长期生成的计算瓶颈。
- **适度的消融分析**：区分了各设计组件的贡献（如CFG是质量旋钮、KV缓存是效率核心、sink帧具有稳定性作用），思路清晰。

## 8. 不足与局限

- **模型和数据规模有限**：相比前沿视频基础模型（如Cosmos、SkyReels-V2），参数量（最大3B）和训练数据规模仍较小，视觉质量上限有限。
- **长期漂移仍然存在**：381帧长程输出中几何、VLM质量和保真度明显退化，复杂交互场景下误差积累问题尚未根本解决。
- **基准覆盖有限**：仅覆盖机器人操作和相机轨迹两个场景，未涉及游戏、自动驾驶、物理仿真等其他重要领域。
- **基线对比不充分**：未与同类自回归模型或大模型后训练方法正面比较，难以定位相对水平。
- **未提及GPU型号及成本参数**：虽然强调"适度算力"，但未具体说明GPU型号，算力评估不够透明。
- **部分设计选择缺乏深入分析**：如sink帧消融呈中性，但其在极长序列上的作用未充分检验；KV缓存压缩后的长期记忆生态未做系统研究。
- **未见人类评估**：虽然包含VLM评估，但缺少对生成视频的自然度、控制精度的用户研究。

（完）
