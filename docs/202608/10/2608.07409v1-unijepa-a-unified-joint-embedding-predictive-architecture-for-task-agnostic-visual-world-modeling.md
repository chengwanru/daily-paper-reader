---
title: "UniJEPA: A Unified Joint-Embedding Predictive Architecture for Task-Agnostic Visual World Modeling"
title_zh: UniJEPA：一种用于任务无关视觉世界建模的统一联合嵌入预测架构
authors: "An Lanji, Dawei Liu, Jin Li, Haoran Xu, Mei Chen, Yu Tian"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07409v1"
tags: ["query:video-wam"]
score: 8.0
evidence: 统一JEPA架构实现跨图像/视频的视觉世界建模
tldr: 联合嵌入预测架构（JEPA）虽为自监督世界建模提供了框架，但现有图像级（I-JEPA）与视频级（V-JEPA）方法相互割裂。UniJEPA提出统一框架，在同一隐空间同时学习光度预测（photometric prediction）与时序预测（temporal prediction），通过共享潜空间实现双重建模。该模型仅需单一目标函数即能防崩溃，无需额外正则化机制，在图像/视频/控制任务上超越专用JEPA，支持零样本规划且推理速度提升数十倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07409v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 727, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1685, \"height\": 948, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 802, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 830, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 826, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 833, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1696, \"height\": 934, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07409v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1479, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1072, \"height\": 461, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1006, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1322, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1776, \"height\": 396, \"label\": \"Table\"}]"
motivation: 现有JEPA框架（如I-JEPA/V-JEPA）针对图像级或视频级建模各自独立，需不同模块与正则化器，阻碍统一世界建模。
method: 构建统一联合嵌入架构，共享隐空间学习光度不变性与时序动态性，仅用下一嵌入预测损失（next-embedding prediction loss）与高斯正则器（Gaussian regularizer）防止崩溃。
result: 跨图像分类、视频预测与控制任务匹配或超越专用JEPAs；零样本规划速度比生成式世界模型快数十倍，精度相当。
conclusion: 单一模型可融合双重建模目标，证明统一隐空间可控抽象（controllable abstraction）能力，且减少超参数依赖，为通用世界模型提供新范式。
---

## 摘要
联合嵌入预测架构（Joint-Embedding Predictive Architectures，JEPAs）已成为在紧凑潜在空间中自监督学习世界模型的原则性框架，但现有方法碎片化：一些在潜空间预测单张图像的掩蔽部分（I-JEPA），另一些学习预测全局光度变换（Image World Models），而视频级 JEPAs 预测未来时间状态并在后训练中实现动作条件规划（如 V-JEPA~2/DINO-World/DINO-WM）。这些目标被视作独立方案，具有分离的编码器、预测器和防坍缩正则化器，阻碍了单一模型统一图像级和视频级世界建模。我们提出 UniJEPA，一种统一的 JEPA，能在共享潜在空间中同时学习光度预测（图像级变换）和时间预测（视频级下一状态动态）。一个端到端的统一目标（由下一嵌入预测损失与高斯正则器构成）产生可证明防坍缩的编码器-预测器对，可直接从原始像素训练而无需 EMA、梯度停止或预训练编码器。实验显示该潜空间支持可控抽象：光度预测学习不变结构而时间预测学习等变动态。在离线轨迹上进行动作条件后训练后，UniJEPA 通过将目标特征视为预测目标实现零样本规划。在图像、视频与控制基准测试中，UniJEPA 匹配或超越专用 JEPA，仅需单一损失超参数，且在同等精度下规划速度比生成式世界模型快数十倍。

## Abstract
Joint-Embedding Predictive Architectures (JEPAs) have emerged as a principled framework for self-supervised learning of world models in compact latent spaces, yet existing methods are fragmented: some predict masked parts of a single image in latent space (I-JEPA), others learn to predict global photometric transformations (Image World Models), while video-scale JEPAs predict future temporal states and are post-trained for action-conditioned planning (V-JEPA~2, DINO-World, DINO-WM). These objectives are treated as distinct recipes with separate encoders, predictors, and anti-collapse regularizers, hindering a single model from unifying image-level and video-level world modeling. We present UniJEPA, a unified JEPA that jointly learns photometric prediction (image-level transformations) and temporal prediction (video-level next-state dynamics) in one shared latent space. A single end-to-end objective, composed of a next-embedding prediction loss and a Gaussian regularizer, yields a provably anti-collapse encoder-predictor pair trainable from raw pixels without EMA, stop-gradient, or pre-trained encoders. We show that the same latent space supports controllable abstraction: photometric prediction learns invariant structure while temporal prediction learns equivariant dynamics. After action-conditioned post-training on offline trajectories, UniJEPA enables zero-shot planning by treating goal features as prediction targets. On image, video, and control benchmarks, UniJEPA matches or surpasses task-specific JEPAs while requiring a single loss hyperparameter, and plans up to tens of times faster than generative world models at comparable accuracy.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义  
- **研究动机**: 现有的联合嵌入预测架构 (Joint-Embedding Predictive Architectures, JEPAs) 在视觉世界建模中存在碎片化问题（fragmentation）。I-JEPA（图像级掩码预测）、Image World Models（画像级光度变换预测）、V-JEPA/DINO-World（视频级时序预测与动作条件规划）等任务需独立训练，各自使用分离的编码器、预测器与防坍缩正则化器（anti-collapse regularizer），无法构建统一的视觉世界模型。  
- **背景意义**: 世界模型（world model）需同时理解场景结构（如光度不变性）与动态演化（如时序等变性），统一框架可提升数据效率（data efficiency）、可迁移性与下游兼容性，但现有方法尚未实现图像级与视频级建模的融合。UniJEPA 旨在解决该问题。  

## 2. 论文提出的方法论  
- **核心思想**: 构建统一架构 **UniJEPA**，在共享潜在空间（shared latent space）中同时优化光度预测（photometric prediction）与时序预测（temporal prediction），采用单一目标函数，并通过高斯正则器（Gaussian regularizer）可证防坍缩（anti-collapse）。  
- **技术细节**:  
  - **共享组件**: 单编码器（ViT backbone）将观测映射为潜在嵌入 \( z \)，单预测器（MLP/ViT）基于条件信号（光度变换参数 \( \tau \) 或动作 \( a_t \)) 预测目标嵌入。  
  - **损失函数**: 统一目标 \( L_{\text{UniJEPA}} = L_{\text{photo}} + L_{\text{temp}} + \alpha R \)。  
    - \( L_{\text{photo}} = \mathbb{E}_{x,\tau} [ \| g_\psi(f_\theta(x), \tau) - f_\theta(\tau(x)) \|_2^2 ] \)（光度预测误差）。  
    - \( L_{\text{temp}} = \mathbb{E}_{(x_t,a_t,x_{t+1})} [ \| g_\psi(f_\theta(x_t), a_t) - f_\theta(x_{t+1}) \|_2^2 ] \)（时序预测误差）。  
    - \( R = \mathbb{E}_{x,u} [ \chi^2_1((u^T z)^2) ] \)（高斯正则器，强制潜分布接近标准正态）。  
  - **防坍缩证明**: 定理 3.1 表明，\( R \leq \varepsilon \) 可保障潜在空间协方差最小特征值 \( \lambda_{\min} \geq 1 - O(\varepsilon) \)，避免 encoder 坍缩为常数。  
  - **可控抽象（controllable abstraction）**: 调整 \( L_{\text{photo}} \) 与 \( L_{\text{temp}} \) 权重可控制表示的不变性（invariance，结构特征）或等变性（equivariance，动态特征）。  
- **算法流程**:  
  - **预训练（pretrain）**: 端到端优化统一损失，从原始像素学习。  
  - **后训练（post-train）**: 冻结编码器，在离线轨迹数据上微调动作条件预测器。  
  - **规划（planning）**: 以目标特征为预测目标，通过模型预测控制（Model Predictive Control, MPC）求解最优动作序列（算法 1）。  

## 3. 实验设计  
- **数据集与场景**:  
  - **图像任务**: ImageNet-1k（线性探测/微调），验证表示质量。  
  - **视频理解**: Something-Something-v2（动作识别）、Epic-Kitchens-100（动作预测）、Perception Test/TempCompass（时序推理诊断）。  
  - **规划任务**: 离线控制套件（迷宫、推送操作、多粒子控制），评估目标到达成功率。  
- **Benchmark 指标**: 线性探测精度（Linear-probe accuracy）、视频 top-1 准确率、Recall@K（动作预测）、规划成功率（Planning success rate）及相对推理速度（Planning speed）。  
- **对比方法**:  
  - **JEPAs**: I-JEPA、IWM、V-JEPA-2、DINO-WM、LeWorldModel。  
  - **生成模型**: Brooks et al. (2024)、Blattmann et al. (2023)。  
  - **对比学习**: SimCLR、DINOv2。  

## 4. 资源与算力  
- 论文未明确说明具体 GPU 型号、数量或总训练时长。  
- 提及：15M 参数的 ViT-Small 在**单张 GPU** 上训练需 **"几小时"**（consistent with LeWorldModel），相较于生成模型，训练收敛步骤减少约 60%（见图 5）。  
- 规划加速归因于：潜在空间预测（无像素解码）、单一损失目标、冻结编码器。  

## 5. 实验数量与充分性  
- **实验数量**:  
  - **主干实验**: 3 类任务（图像、视频、规划）覆盖 6+ 数据集，合计 10+ 定量指标。  
  - **消融实验（ablation study）**:  
    - 损失组件（移除光度/时序损失、正则器消融）。  
    - 正则器权重 \( \alpha \) 敏感性分析（见图 7）。  
    - 模型规模（ViT-Small vs. ViT-Large）。  
    - 规划配置（MPC 时间步 \( H \) 、候选序列数 \( C \)）。  
  - 8+ 对比基线，跨越架构差异（潜在空间 vs. 像素空间）。  
- **充分性与公平性**:  
  - 覆盖多样任务与规模，展示跨场景鲁棒性。  
  - 消融实验系统验证关键组件贡献（如正则器必要性）。  
  - 基准对比包括同参数规模模型（ViT-S/L）和控制变量（如 H,C）：结果未偏向特定设定，体现客观性。  

## 6. 主要结论与发现  
- **核心结论**: UniJEPA 在**单一共享潜在空间**实现了图像级与视频级世界建模的统一，端到端训练且仅需**单一损失超参数**（\( \alpha \)），超越或匹配专用 JEPAs。  
- **关键发现**:  
  - 在 ImageNet 线性探测精度达 **74.9%**（优于 IWM 的 73.5%），视频任务 top-1 达 **78.1%**（优于 V-JEPA-2 的 77.3%）。  
  - 零样本规划成功率 **75.8%** 且加速 **44 倍**（vs. 生成模型）。  
  - 正则器可证防坍缩，无需 EMA（Exponential Moving Average）或梯度停止（stop-gradient）。  
  - 损失权重调整可**可控抽象**（invariance-equivariance spectrum）。  

## 7. 优点  
- **方法亮点**:  
  - **统一性**: 共享潜在空间支持双重建模目标，减少系统复杂性。  
  - **高效性**: 高斯正则器取代启发式防坍缩机制，训练收敛更快；潜在空间规划规避像素解码开销。  
  - **理论保障**: 正则器的防坍缩特性可严格证明。  
- **实验亮点**:  
  - 跨模态任务验证统一表示通用性。  
  - 消融实验定量揭示损失组件作用（如表 2）。  
  - 成本-精度帕累托优势显著（图 6）。  

## 8. 不足与局限  
- **方法局限**:  
  - 共享预测器可能限制**长时域预测能力**或处理**多模态任务**（如语言指令）。  
  - 高斯正则器虽防坍缩，但可能抑制极端尺度下的**表示容量**。  
- **实验覆盖**:  
  - 未涵盖**语言驱动**场景，扩展性依赖后续多模态整合（如 vision-language-action 模型）。  
  - 规划场景限于**视觉目标到达**，未测试高层语义任务。  
- **风险**:  
  - 动态预测存在**模糊性风险**（如遮挡场景），规划可靠性需部署前严格验证（Impact Statement 强调）。  
- **应用限制**: 需离线轨迹数据进行后训练，零样本规划依赖于潜在空间目标嵌入相似性假设。  

（完）
