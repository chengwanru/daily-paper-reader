---
title: "Robust-WAM: Bridging Generative Pretraining and Semantic Foresight in World-Action Models"
title_zh: Robust-WAM：桥接世界动作模型中的生成式预训练与语义预见
authors: "Haodong Yan, Junfeng Li, Junjie He, Zhide Zhong, MingMing Yu, Wenxuan Song, Jiaguan Zhu, Yangyang Zheng, Yuqiao Du, Jiadi You, Yingjie Cai, Xu Yan, Guanyi Zhao, Bingbing Liu, Haoang Li"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05903v1"
tags: ["query:video-wam"]
score: 9.0
evidence: 提出针对基于视频生成模型的世界动作模型的后训练方法，直接匹配需求项。
tldr: 现有基于视频生成的WAM在VAE潜在空间利用预训练，但动作预测受外观变化干扰；语义空间虽鲁棒却无法利用大模型预训练。Robust-WAM提出后训练方案，保留VAE生成路径，并用可学习查询令牌将未来场景语义对齐到动作流。在分布外模拟与真实机器人上，多个WAM基线成功率一致提升，且分布内性能不降。该方法弥合了生成预训练与语义预视的鸿沟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05903v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 943, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05903v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05903v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1715, \"height\": 666, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.05903v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 862, \"height\": 514, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 862, \"height\": 420, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1721, \"height\": 300, \"label\": \"Table\"}]"
motivation: VAE潜在空间优化像素重建导致动作预测脆弱，而语义空间无法利用VGM预训练，需兼顾两者。
method: 保留VAE生成路径，添加轻量级语义预视对齐，用可学习查询令牌与未来帧语义对齐，位置编码对应动作令牌。
result: 在OOD模拟和真实机器人上，多个WAM基线成功率一致提升，且不损失分布内性能。
conclusion: 证明生成预训练与语义鲁棒性可兼得，为WAM提供外观不变的行动基础。
---

## 摘要
主流世界动作模型（WAMs）采用预训练的视频生成模型（VGMs）进行机器人控制，将其学习到的动力学先验迁移到动作预测中。这些VGM通常在变分自编码器（VAE）潜在空间中训练。然而，VAE潜在空间是为像素重建而优化的，这奖励精细的外观细节，使得动作预测在视觉变化下变得脆弱。最近的工作在语义潜在空间中构建WAMs，这些模型对外观变化更加鲁棒。然而，这些模型无法利用仅存在于VAE空间的大规模VGM预训练。为了克服这一困境，我们提出了Robust-WAM，一种用于基于视频生成的WAMs的通用后训练方法，它保留了基于VAE的生成路径，并在动作流上添加了一个轻量级的语义预见对齐目标。这保留了大规模VGM预训练，同时将动作建立在不受外观影响的动力学上，这些动力学在光照变化和其他视觉分布外条件下保持可靠。具体来说，我们采用可学习的查询标记，通过将其输出隐藏状态与未来真实帧的语义预见对齐，将未来场景语义引入动作流。为了建立每个查询与其描述的未来步骤之间的时间对应关系，我们为其提供匹配动作标记的位置编码。在分布外泛化模拟基准和真实机器人设置上的实验表明，我们的Robust-WAM持续提高了多个WAM基线的成功率，而不会牺牲分布内性能。

## Abstract
Mainstream World-Action Models (WAMs) adapt pretrained video generation models (VGMs) for robot control, transferring their learned dynamics prior for action prediction. These VGMs are typically trained in a variational autoencoder (VAE) latent space. However, the VAE latent space is optimized for pixel reconstruction, which rewards fine appearance detail and leaves the action prediction fragile under visual shifts. Recent works build WAMs in semantic latent space, which are more robust to appearance shifts. However, these models cannot leverage the large-scale VGM pretraining that exists only in VAE space. To overcome this dilemma, we propose Robust-WAM, a general post-training method for video-generation-based WAMs that preserves the VAE-based generative path and adds a lightweight semantic foresight alignment objective on the action stream. This retains the large-scale VGM pretraining while grounding actions in appearance-invariant dynamics that stay reliable under illumination shifts and other visual out-of-distribution conditions. Specifically, we employ learnable query tokens to bring future-scene semantics into the action stream by aligning their output hidden states with the semantic foresight of future ground-truth frames. To establish the temporal correspondence between each query and the future step it describes, we give it the positional encoding of the matching action tokens. Experiments on out-of-distribution generalization simulation benchmarks and a real-robot setup show that our Robust-WAM consistently improves the success rates of multiple WAM baselines without sacrificing in-distribution performance.

---

## 论文详细总结（自动生成）

# Robust-WAM 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：世界动作模型（WAMs）通过预训练的视频生成模型（VGMs）获取动力学先验，用于机器人动作预测。主流 WAMs 在 VAE 潜在空间中运行，该空间为像素重建而优化，保留了大量外观细节（纹理、光照等）。
- **核心矛盾**：
  - **VAE 空间 WAMs**：继承了大规模 VGM 预训练的动力学先验，但动作预测对视觉分布外（OOD）变化（光照、颜色、背景等）非常脆弱。
  - **语义空间 WAMs**：在 DINO、V-JEPA 等语义潜在空间中建模，对外观变化鲁棒，但无法利用仅存在于 VAE 空间的大规模 VGM 预训练，需要昂贵的数据重训。
- **问题**：如何在不放弃大规模生成预训练的前提下，获得外观不变的动力学表征？
- **意义**：Robust-WAM 提出一种通用后训练方法，同时兼顾强生成先验与语义鲁棒性，为 WAMs 在真实复杂环境中的部署提供了一条低成本路径。

## 2. 论文提出的方法论

### 核心思想
- **不替换潜在空间**，而是保留原 WAM 的 VAE 生成路径，仅在**动作流**上添加轻量级的“语义预见对齐”目标，纠正外观偏差。
- 利用冻结的 DINOv3 CLS 嵌入作为**外观不变的语义目标**，让动作流获得对未来场景的语义级预见能力。

### 关键技术细节
1. **可学习查询令牌（Query Tokens）**：
   - 对每个未来时间步和每个相机视角，在动作序列前添加一个可学习查询令牌。
   - 查询令牌与动作令牌一起输入 Action DiT，通过注意力机制交互。
2. **语义对齐目标**：
   - 用冻结的 DINOv3 提取未来真实帧的 CLS 嵌入作为目标。
   - 查询令牌的输出经线性头映射到目标空间，计算余弦相似度损失 \( L_{\text{align}} \)。
   - 总损失为原 WAM 损失加上对齐损失：\( L = L_{\text{WAM}} + \lambda_{\text{align}} L_{\text{align}} \)。
3. **时间位置编码共享**：
   - 每个查询令牌复用其对应动作步骤的位置编码（PE），明确“该查询描述的是哪个未来时刻”。
   - 未来帧 j 对应动作步 \( \tau_j = \min(j\Delta, L) \)，使查询与动作时序对应。
4. **推理开销为零**：
   - 训练时使用 DINOv3 教师和线性头；推理时丢弃它们，仅保留查询令牌，原动作流结构不变。

### 算法流程（文字说明）
- 给定当前观测、语言指令，WAM 同时去噪未来帧的 VAE 潜变量和动作块。
- 在动作流中，将查询令牌与带噪动作令牌拼接，注入共享位置编码，由 Action DiT 处理。
- 训练时，查询输出对齐到 DINOv3 的未来帧 CLS 嵌入；动作输出通过原动作头生成动作。
- 优化目标为视频流损失 + 动作流损失 + 语义对齐损失。

## 3. 实验设计

### 数据集与 Benchmarks
- **LIBERO-Plus**：基于标准 LIBERO 套件，包含 7 种扰动轴（布局、相机、初始状态、语言、光照、背景、传感器噪声），评估 OOD 泛化。
- **RoboTwin clean → random**：在干净演示上训练，在随机化拆分（物体姿势、纹理、光照变化）上评估。
- **真实机器人**：Franka Research 3 平台，三种桌面任务（胡萝卜→面包、猕猴桃→篮子、叠杯子），只改变光照（紫色和青色）。

### 对比方法
- **VLA 类**：OpenVLA、WorldVLA、NORA、UniVLA、π0、π0-FAST、OpenVLA-OFT、StarVLA、GR00T-N1.7。
- **WAM 类**：
  - VAE 空间：FastWAM、GE-Act、LingBot-VA（作为基线）。
  - 语义空间：LDA-1B。

### 基线配置
- 对每个 WAM，Baseline 和 Robust-WAM 变体共享相同训练数据、调度和优化设置，以保证公平性。

## 4. 资源与算力

- **论文未明确说明使用的 GPU 型号、数量或具体训练时长**。
- 仅提到某些训练步数（如 LingBot-VA 在 RoboTwin 上训练 15K 步），但未提供计算资源细节。
- 语义目标（DINOv3 CLS）为离线提取，推理时无需额外成本。

## 5. 实验数量与充分性

### 实验数量
- **两个模拟 OOD 基准**（LIBERO-Plus、RoboTwin clean→random）。
- **三个 WAM 架构**（FastWAM、GE-Act、LingBot-VA），涵盖 action-expert 和 unified 两种类型。
- **消融实验**：对齐目标（DINOv3 CLS / Patch / DA3 / CLS+DA3）、查询设计（无时间 PE、当前帧目标 vs 未来帧目标）。
- **真实机器人实验**：三类任务 × 两种 OOD 光照 + 标准光照，每任务 25 次 rollout。

### 充分性评价
- **优点**：覆盖多种架构、多种扰动类型、模拟+真实，且设置了消融，实验相对充分。
- **客观性**：统一训练配置、遵循各 WAM 官方食谱，对比方法引用原始结果，总体上公平。
- **局限**：真实机器人仅测试光照变化，未覆盖其他 OOD 维度；消融只在 FastWAM 上进行，结果可能不完全适用于其他架构；无推理时间或参数量对比。

## 6. 论文的主要结论与发现

- Robust-WAM 在 **LIBERO-Plus** 上显著提升 FastWAM（+9.2 点）和 GE-Act（+2.9 点）的成功率，同时 **不损失甚至小幅提升** 分布内性能。
- 在 **RoboTwin** 上，Robust-WAM 将 LingBot-VA 的随机化成功率从 29.8% 提升到 34.4%，且保持 clean 成功率不变。
- **语义表示最好用于增强，而非替代预训练 VGM 的动力学先验**：与 LDA-1B 相比，结合 VAE 先验+语义对齐的模型显著优于纯语义模型。
- 消融实验显示：
  - DINOv3 CLS 作为单一目标最佳；
  - 时间位置编码（未来步索引）和未来语义目标都必不可少。
- 在真实机器人上，Robust-WAM 将 GE-Act 的 OOD 成功率从 57.3% 提升到 80.0%，InD 到 OOD 的差距从 22.7 点缩小到 2.7 点。

## 7. 优点

- **保留大规模预训练的优势**：不替换 VAE 空间，避免了昂贵的重新预训练。
- **轻量级后训练**：仅增加少量查询令牌和线性头，推理时无额外计算。
- **通用性强**：可应用于 action-expert（GE-Act、FastWAM）和 unified（LingBot-VA）等多种 WAM 架构。
- **设计合理性**：查询令牌与动作令牌共享位置编码，建立精确的时间对应，优于没有时间归纳偏置的方法。
- **实验验证充分**：在模拟和真实环境中均证明 OOD 鲁棒性提升，且不牺牲 InD 性能。

## 8. 不足与局限

- **真实实验覆盖有限**：仅测试了光照变化，未涉及相机视角、背景、传感器噪声等更广的 OOD 场景。
- **负结果未充分解释**：在 LIBERO-Plus 上 FastWAM 的“初始状态”轴有轻微下降，GE-Act 的“光照”轴小幅下降，论文未提供深入分析。
- **对齐目标的选择**：消融显示 DINOv3 CLS 最佳，但并未与其他语义模型（如 DINOv2、V-JEPA）对比，也未说明为什么 CLS 优于 Patch 或深度特征。
- **计算开销未报告**：训练时间、显存占用等没有量化，实际部署前的成本难以评估。
- **可能存在的偏差**：所有 WAM 变体共享原食谱的超参数，但查询数量、对齐权重（λ=0.1）是否对所有架构最优未做专门调优，可能存在架构特定性。

（完）
